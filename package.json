{
  "name": "zm-palette-generator",
  "version": "1.0.0",
  "description": "For a given color, it generates a palette of similar colors",
  "main": "dist/main.js",
  "repository": "Zimbra/zm-palette-generator",
  "scripts": {
    "build": "webpack -p",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  },
  "dependencies": {
    "@ctrl/tinycolor": "^2.6.0"
  }
}
